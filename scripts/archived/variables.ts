import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  spacing: 8,
  palette: {
    primary: {
      main: '#3b82f6',
    },
    secondary: {
      main: '#2563eb',
    },
  },
});

export default theme;
