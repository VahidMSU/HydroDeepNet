import React from 'react';
import ModelSettingsTemplate from '../components/templates/ModelSettings'; // Import the new ModelSettingsTemplate component

const ModelSettings = () => {
  return <ModelSettingsTemplate />;
};

export default ModelSettings;
